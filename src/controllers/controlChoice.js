export function postChoice(req, res){
    // Valida o _id e o title (ñao pode haver dois); é a função que cria as opções a serem votadas na enquete
    // salva no db e retorna
}

export function postChoiceVote(req, res){
    // Valida a enquete e o voto
    // Salva na DB e retorna o status
}